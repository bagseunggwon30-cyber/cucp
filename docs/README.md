# Documentation Assets

This directory keeps documentation-only assets for the public README and
reference pages.

The demo GIF under `assets/` is intentionally separated from source scripts and
tests so the root repository view stays easy to scan.
