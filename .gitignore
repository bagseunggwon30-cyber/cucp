# CUCP runtime/cache files created during local runs.
*.log
*.ndjson
wrapper-cache/
appshot-*.json
appshot-*.png
screenshots/
trajectory.ndjson
smart-click-history.ndjson

# OS/editor metadata.
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Python scratch environments.
__pycache__/
*.pyc
.venv/
venv/

# Node dependencies if future helper tooling is added.
node_modules/

# Secret-bearing local files.
.env
.env.local
*.key
*.pem
secrets/
credentials/

# Build/test temp.
*.tmp
*.bak
*.pester-out

# Test scratch files.
tests/_*.ps1
tests/scratch-*.ps1
tests/.*.ps1
tests/.*.txt

# Live verification cassettes may include local window titles, URLs, or screen
# metadata. Regenerate them locally instead of committing them.
live-verify/
